# Momentum Watchlist Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# DATA PROVIDER (default: yfinance - FREE, no API key needed!)
# =============================================================================
PROVIDER_NAME=yfinance

# =============================================================================
# OPTIONAL: Alpaca Markets API Credentials (only if using Alpaca provider)
# =============================================================================
# Get your API keys from https://alpaca.markets/
# Uncomment and fill in if you want to use Alpaca instead of yfinance

# PROVIDER_NAME=alpaca
# APCA_API_KEY_ID=your_alpaca_api_key_id
# APCA_API_SECRET_KEY=your_alpaca_api_secret_key
# APCA_BASE_URL=https://paper-api.alpaca.markets
# APCA_DATA_URL=https://data.alpaca.markets

# =============================================================================
# REQUIRED: SendGrid Email Settings
# =============================================================================
# Get your API key from https://sendgrid.com/

SENDGRID_API_KEY=your_sendgrid_api_key
FROM_EMAIL=watchlist@yourdomain.com
TO_EMAIL=your.email@example.com

# =============================================================================
# OPTIONAL: Scanner Settings
# =============================================================================
# These have sensible defaults, override if needed

# Minimum stock price to include (default: 5)
# MIN_PRICE=5

# Minimum volume so far to include (default: 1000000)
# MIN_VOLUME=1000000

# Number of top picks to include in email (default: 5)
# PICKS=5

# Number of candidates to seed from movers (default: 100)
# TOP_N_SEED=100

# =============================================================================
# OPTIONAL: Time Gate Settings
# =============================================================================
# Usually don't need to change these

# Target hour in CT for execution (default: 8)
# TARGET_HOUR=8

# Target minute for execution (default: 40)
# TARGET_MINUTE=40

# Minutes tolerance for execution window (default: 2)
# EXECUTION_WINDOW_MINUTES=2

# =============================================================================
# OPTIONAL: Email Behavior
# =============================================================================

# Send email when market is closed (default: false)
# SEND_MARKET_CLOSED_EMAIL=false

# =============================================================================
# OPTIONAL: Provider Settings
# =============================================================================

# Data provider name (default: alpaca)
# PROVIDER_NAME=alpaca

# Data delay type: realtime, delayed, or leave empty for unknown
# DATA_DELAY_TYPE=delayed

